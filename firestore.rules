rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Bloqueamos acceso directo desde el navegador (Frontend)
      // Tu Backend seguirá entrando porque tiene permisos de Admin
      allow read, write: if false;
    }
  }
}